import excludePatternEverywhere from './rules/forbid-pattern-everywhere';
import noEmptyBlockComment from './rules/no-empty-block-comment';

export const rules = {
    excludePatternEverywhere,
    noEmptyBlockComment,
};
